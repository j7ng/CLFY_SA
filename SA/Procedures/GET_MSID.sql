CREATE OR REPLACE PROCEDURE sa."GET_MSID"

(

  PARAM1 IN VARCHAR2

, PARAM2 OUT VARCHAR2

) AS

BEGIN

SELECT  msid into Param2

FROM IG_TRANSACTION

Where Esn = PARAM1

AND creation_date >= TRUNC(SYSDATE-120)

ORDER BY CREATION_DATE;

END GET_MSID;
/