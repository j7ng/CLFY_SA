CREATE OR REPLACE TYPE sa.TYP_DEVICE_PREF AS OBJECT (ESN         VARCHAR2(50),
                                                  MIN         VARCHAR2(50),
                                                  PREFERENCE  VARCHAR2(1));
/